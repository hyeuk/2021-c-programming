c애플리케이션

솔루션 : 하나 이상의 프로젝트를 저장,관리하는 단위(예 : 폴더)

주석

/* 와 */ 사이는 모두 주석 ex) /*............*/

한 줄 주석 : //다음한줄끝까지 주석 ex) // ...........

전처리 지시자
#으로 시작하는 문장
컴파일 하기 전에 먼저 처리해야 할 일을 수행하도록 명력을 내리는 문장

문장#include는 다음에 나오는 헤더 파일(header file)을 이
문장이 있는 부분에 삽입하는 역할을 수행

#include <헤더파일이름>
#include  "헤더파일이름"

시스템 헤더파일

#include <stdio.h>

stdio.h : 표준 입출력 작업

줄구분,들여쓰기

들여쓰기를 안할경우 프로그램의 이해력을 떨어트림

적절한 줄 구분과 빈 줄 삽입, 들여쓰기는
프로그램의 이해력을 돕는데 매우 중요!!한 요소

변수

변수는 자료값을 저장하는 공간

변수 선언

변수 자료형과 변수 이름을 나열하여 표시 예) : int a ;  a=10;

변수 저장장소와 값

l-value : 대입연산자 '='의 왼쪽에 위치함
저장장소를 의미함(변수만 가능)

r-value : 대입연산자 '='의 오른쪽에 위치함
값을 의미함(상수,변수,수식,함수 등 모두 가능)

기본 자료형 3가지

문자형 : char

정수형 : int

실수형 : double

매크로상수(기호상수)

#define
전처리 지시자인 #define은 소스에서 지정된 기호상수나 매크로를 정의된
치환문자열로 대체(치환)하는 역할 수행

장점

지시자 #define은 시스템에 따라 변할수있는 상수를 한번에 수정할수
있고 , 수정되는 한 부분만 검사하면 되므로 이식성과 신뢰성이 증대

#define  PI  3.14	//매크로상수
#define  SQUARE(x)    ((x)*(x))           //인자가 있는 매크로 상수	

r=3 ;
s = PI  *  SQUARE(r) ;

오버플로 : 숫자가 넘어가는경우
